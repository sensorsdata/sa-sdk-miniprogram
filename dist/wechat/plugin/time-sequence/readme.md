# 事件的时间间隔

## 功能
集成插件后，可以保证事件不会在同一时间触发。事件的 time 和下个事件的 time 肯定会有间隔，保证所有的事件是有顺序的，有时间间隔的。
就算真的是同一时间触发的事件，也会被改成不同时间。原理是会对比上次事件的时间，如果当前时间相同或者小，就把当前事件的时间改成历史时间加 1 毫秒。
这个是内置插件，不需要额外引入。SDK 默认已经引入。
这个功能是特意针对神策后端做的优化，为了满足神策后端必须要保证同一时间不能出现多个事件。
